[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m streamlit run self_improving_rag/dashboard_entry.py --server.address 0.0.0.0 --server.port $PORT --server.headless true"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
